PACMAN.UI.Pinky = function(ctx, spec){
    spec.s    = 18;
    spec.top  = 10 * PACMAN.SIZE;
    spec.left = 8 * PACMAN.SIZE;
    spec.color = PACMAN.PINK;
    that = PACMAN.UI.Ghost(ctx,spec);
    
};
