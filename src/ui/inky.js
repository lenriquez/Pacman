
PACMAN.UI.Inky = function(ctx, spec){
    spec.s    = 18;
    spec.top  = 10 * PACMAN.SIZE;
    spec.left = 9 * PACMAN.SIZE;
    spec.color = PACMAN.ORANGE;
    that = PACMAN.UI.Ghost(ctx,spec);
    
};
