
PACMAN.UI.Clyde = function(ctx, spec){
    spec.s    = 18;
    spec.top  = 10 * PACMAN.SIZE;
    spec.left = 10 * PACMAN.SIZE;
    spec.color = PACMAN.BLUE;
    that = PACMAN.UI.Ghost(ctx,spec);
    
};

